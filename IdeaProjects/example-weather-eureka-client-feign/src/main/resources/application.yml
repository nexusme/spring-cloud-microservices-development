spring:
  application:
    name: example-weather-eureka-client-feign
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8888/eureka/
feign:
  client:
    config:
      feignName:
        connectTimeout: 5000
        readTimeout: 5000